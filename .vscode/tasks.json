{
    "tasks": [
        {
            "type": "shell",
            "label": "Build rabbit",
            "command": "bazel build -c dbg //:rabbit",
            "options": {
                "cwd": "${workspaceFolder}",
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}